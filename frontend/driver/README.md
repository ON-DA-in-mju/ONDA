# ON-DA Driver (명지대 자연캠퍼스 셔틀)

기사님용 ON-DA 앱 — **Kotlin + Jetpack Compose**

## 열기

Android Studio에서 이 폴더(`frontend/driver`)를 Open 하세요.

```bash
cd frontend/driver
./gradlew :app:assembleDebug
```

## 기타

- `png/` — 화면 설계 PNG
- `tools/` — 이미지 크롭 등 유틸 스크립트
